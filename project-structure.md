# Prompt Pocket - Project Structure

```
prompt-pocket/
├── manifest.json                # Manifest V3 configuration
├── package.json                 # Dependencies and build scripts
├── tsconfig.json                # TypeScript configuration
├── webpack.config.js            # Build configuration
├── README.md                    # Project documentation
│
├── src/
│   ├── global.d.ts              # Ambient declaration for the build-time DEBUG flag
│   │
│   ├── types/
│   │   ├── index.ts             # Barrel re-exporting storage + message types
│   │   ├── storage.ts           # Storage-related interfaces and defaults
│   │   └── messages.ts          # Message and response type definitions
│   │
│   ├── background/
│   │   └── background.ts        # Service worker
│   │
│   ├── content/
│   │   ├── contentScript.ts     # Main content script
│   │   ├── uiInjector.ts        # Floating UI injection logic
│   │   └── platforms/
│   │       ├── index.ts                  # Public surface of the platform layer
│   │       ├── basePlatformDetector.ts   # PlatformDetector interface + shared types
│   │       ├── detectorFactory.ts        # Picks the detector matching the current page
│   │       ├── detectorUtils.ts          # Shared input lookup and fill helpers
│   │       ├── chatGPTDetector.ts
│   │       ├── claudeDetector.ts
│   │       ├── geminiDetector.ts
│   │       └── doubaoDetector.ts
│   │
│   ├── popup/
│   │   ├── popup.tsx            # React popup root
│   │   ├── components/
│   │   │   ├── PromptList.tsx
│   │   │   ├── PromptEditor.tsx
│   │   │   ├── CategoryManager.tsx
│   │   │   ├── FilterDropdown.tsx
│   │   │   ├── VariableForm.tsx
│   │   │   └── Settings.tsx
│   │   └── styles/
│   │       └── popup.css
│   │
│   ├── storage/
│   │   ├── storage.ts           # Storage abstraction
│   │   └── migrations.ts        # Data migration and normalization
│   │
│   └── utils/
│       ├── constants.ts         # UI element IDs, z-index layers, animation timings
│       ├── messages.ts          # sendMessage helper and MessageBuilder
│       ├── dom.ts               # DOM helpers (createElement, debounce, ...)
│       ├── variables.ts         # {{name}} placeholder parsing and substitution
│       └── theme.ts             # Theme resolution and system-theme watching
│
├── static/
│   ├── icons/                   # Generated by `npm run build:icons` from assets/logo.png
│   ├── popup.html               # Popup HTML file
│   └── styles/
│       └── global.css           # Styles injected into AI chat pages
│
├── assets/                      # Source logo and store listing images
│
└── dist/                        # Build output directory (git-ignored)
```

There is no automated test suite yet; see the Testing section in `README.md`.

## Key Files Description:

### Core Files:
- `manifest.json`: Manifest V3 configuration with minimal required permissions
- `src/background/background.ts`: Service worker; the single owner of storage access
- `src/content/contentScript.ts`: Content script injected into supported AI chat pages
- `src/popup/popup.tsx`: React-based popup UI for prompt management
- `src/storage/storage.ts`: Abstraction over chrome.storage.local with type safety

### Supporting Modules:
- `src/types/`: TypeScript interfaces for type safety
- `src/utils/`: Utility functions shared across modules
- `src/content/platforms/`: One detector per AI platform behind a common interface,
  plus the factory that selects the right one for the current page
- `src/content/uiInjector.ts`: Floating UI injection and management

### Static Assets:
- `static/popup.html`: HTML entry point for popup
- `static/icons/`: Extension icons for different sizes
- `static/styles/global.css`: Global CSS styles

### Build Configuration:
- `package.json`: Dependencies and scripts
- `tsconfig.json`: TypeScript compiler settings
- `webpack.config.js`: Bundling configuration; defines the compile-time `DEBUG` flag
